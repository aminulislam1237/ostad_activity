import 'package:flutter/material.dart';
import 'package:ostad_activity/home_screen.dart';

class watertrackerapp extends StatelessWidget {
  const watertrackerapp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: homescreen() ,
    );
  }
}
